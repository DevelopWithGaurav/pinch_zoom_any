name: pinch_zoom_any
description: >
  Add pinch-to-zoom, pan, double-tap zoom, and double-tap reset to any
  Flutter widget. Zero dependencies — pure Flutter.
version: 0.0.1
homepage: https://github.com/DevelopWithGaurav/pinch_zoom_any
repository: https://github.com/DevelopWithGaurav/pinch_zoom_any
issue_tracker: https://github.com/DevelopWithGaurav/pinch_zoom_any/issues

environment:
  sdk: ^3.11.4
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
